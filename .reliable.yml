
sudo: required

language: node_js

addons:
  brew:
    - autojump

env:
  mirror:
    nvm: https://npm.taobao.org/dist
    npm: http://registry.npm.com

node_js: 
  - 6

services: docker

before_install:
  - nvm ls

before_script: nvm ls

script: make test-ios
after_script: npm -v

after_failure: npm -v

after_success: